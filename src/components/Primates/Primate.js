export const Primate = ( {primate} ) => {
    return ( 
    
            <section className="primate" key={primate.id}>
               <header className="primate-info"></header>
                    <div>{primate.name}</div>
                    </section>

                    )
         
}